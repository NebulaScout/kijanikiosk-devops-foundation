KijaniKiosk CI/CD Pipeline Credential Audit 

Date:   2026-07-23
Auditor: Automated scan (manual review of results)
Scope:  Jenkinsfile, git commit history, all build/terminal logs, Terraform files, .env


1. JENKINSFILE

File: Jenkinsfile (170 lines, branch feature/week5-ci-pipeline)

Hardcoded credential scan:
  grep -niE 'password|secret|token|api_key|AKIA|private_key|credential=' Jenkinsfile
  RESULT: No hardcoded credentials found.

The only credential references are:
  Line 126: withCredentials([usernamePassword(
              credentialsId: 'nexus-credentials-id',
              usernameVariable: 'NEXUS_USER',
              passwordVariable: 'NEXUS_PASS')])

This is the correct pattern:
  - credentialsId 'nexus-credentials-id' is a Jenkins credential-store key, not a secret value.
  - NEXUS_USER and NEXUS_PASS are runtime environment variables injected by Jenkins, never written to the file.
  - The withCredentials block scopes the secrets to a single shell invocation; they are not persisted.

Environment block (lines 11-18):
  NODE_ENV=test, BUILD_DIR=dist, APP_NAME=kijanikiosk, NEXUS_URL=http://host.docker.internal:8081,
  NEXUS_REPO_NAME=npm-kijanikiosk, ARTIFACT_NAME=kijanikiosk
  All non-secret configuration values.

VERDICT: PASS - No credentials in the Jenkinsfile.


2. GIT COMMIT HISTORY

Commits scanned: 32 (all branches, full history)

Commands run:
  git ls-files | grep -iE '\.env|\.tfvars|\.pem|\.key|password|secret|credential'
    git log --all -p -S 'AKIA' -- '*.env' '*.tfvars' 'terraform/*.tf'
  git log --all -p -S 'secret_key' -- '*.env' '*.tfvars'
  git log --all --name-only --diff-filter=A (looking for sensitive file additions)

Results:
  - No .env, .tfvars, .pem, or .key files are tracked by git.
  - No AWS access key IDs (AKIA...) appear in any commit diff.
  - No secret keys or passwords appear in any commit diff.
  - Sensitive files (.env, *.tfvars) are listed in .gitignore and were never committed.

VERDICT: PASS - No credentials leaked in git history.


3. BUILD LOGS

Files scanned:
  green-pipeline-run.txt   (Jenkins build #1, successful run)
  pipeline-run1.log        (Infrastructure pipeline run 1)
  pipeline-run2.log        (Infrastructure pipeline run 2, idempotent re-run)
  terminal.output.log      (Local terminal session log)

green-pipeline-run.txt findings:
  Line 197: "Masking supported pattern matches of $NEXUS_PASS" - confirms Jenkins masking is active.
  Line 202: "+ echo -n admin:****" - password masked with asterisks.
  Line 204: "+ NEXUS_AUTH_TOKEN=YWRtaW46****" - base64 token masked with asterisks.
  Line 209: "...YWRtaW46****" - token value masked in printf output.

  The actual credential values are never visible in the log. Jenkins correctly identified
  and masked $NEXUS_PASS before printing.

pipeline-run1.log: No credential matches.
pipeline-run2.log: No credential matches.
terminal.output.log: No credential matches.

VERDICT: PASS - No credentials exposed in any build or terminal log.


4. TERRAFORM AND ENV FILES (local only, not committed)

terraform/terraform.tfvars contains:
  aws_access_key = "A****"
  aws_secret_key = "Ij****t"

.env contains:
  access_key = AKI*****
  secret_key = Ij****

These files exist locally on the workstation but:
  - Both are in .gitignore (*.tfvars and .env patterns).
  - Neither file is tracked by git (git ls-files returns empty for both).
  - Neither file has ever been committed (git log shows no history for these paths).
  - terraform/main.tf references var.aws_secret_key as a variable, not a literal value.

VERDICT: PASS - Credentials exist only in local ignored files, never in version control.


5. TERRAFORM .tf FILES

terraform/main.tf line 13: secret_key = var.aws_secret_key
terraform/variables.tf line 12-14: variable "aws_secret_key" { description = "AWS secret key" }

These are variable declarations, not secret values. The actual secret is supplied at runtime
via terraform.tfvars (which is gitignored) or environment variables.

VERDICT: PASS - No credential values in Terraform source files.


SUMMARY
=======
Source                    Credentials Found    Leaked to Version Control
------------------------  -------------------  -------------------------
Jenkinsfile               None (uses store)    N/A
Git commit history        None                 N/A
Build logs                None (masked)        N/A
Terraform .tf files       Variable refs only   N/A
terraform.tfvars          Yes (local only)     No — gitignored, never committed
.env                      Yes (local only)     No — gitignored, never committed

OVERALL RESULT: PASS - No credentials appear in any committed file, build log, or pipeline definition.
