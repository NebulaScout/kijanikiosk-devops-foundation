apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

configMapGenerator:
  - name: kk-payments-config
    envs:
      - kk-payments.env

labels:
  - pairs:
      kijani.io/environment: production
    includeSelectors: false
